<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	<!-- Spring MVC 扫描的包 -->
	<context:component-scan base-package="org.pan.springmvc"></context:component-scan>

	<!-- 配置视图解析器 -->
	<bean id="internalResourceViewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- 国际化信息配置, 注意id 名称一定要是 messageSource， 如果是resourceBundleMessageSource，将无法支持 i18n 解析 -->
	<bean id="messageSource"
		class="org.springframework.context.support.ResourceBundleMessageSource">
		<property name="basename" value="i18n" /> <!-- 国际化信息所在的文件名称 -->
	</bean>
	
	<!--
		不需要通过Controller，直接访问 view; 慎用view-controller，因为绕过了Handler
		path：访问的路径
		view-name: 访问视图的的名字
		如果光配置这个，会影响所有的Controller。所以需要添加 mvc:annotation-driven
	  -->
	<mvc:view-controller path="/pan/pan" view-name="ok"/>
	<mvc:annotation-driven></mvc:annotation-driven>

	<!-- 自定义视图解析器，需要配置  BeanNameViewResolver -->
	<bean id="" class="org.springframework.web.servlet.view.BeanNameViewResolver">
		<!-- 只要不是比 Integer.MAX_VALUE 大，都是先查询返回的 view  -->
		<property name="order" value="10"></property>
	</bean>
	
</beans>
