<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
		http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

	<!-- 扫描包的路径 -->
	<context:component-scan base-package="org.pan.springmvc" />

	<bean id="internalResourceViewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- 处理静态资源 -->
	<mvc:default-servlet-handler />
	<!-- 添加注解<mvc:annotation-driven></mvc:annotation-driven> 会默认装配好一个 LocalValidatorFactoryBean -->
	<mvc:annotation-driven></mvc:annotation-driven>
	<!-- <mvc:annotation-driven conversion-service="formattingConversionServiceFactoryBean"></mvc:annotation-driven> -->

	<!-- 配置自定义的类型转换器，并且在 annotation-driven 中注册 -->
	<!-- 默认配置的 格式化+转换 的 FactoryBean, 所以我们在用到 格式化 注解的时候，不能使用： class="org.springframework.context.support.ConversionServiceFactoryBean" 
		需要使用: org.springframework.format.support.FormattingConversionServiceFactoryBean -->
	<bean id="formattingConversionServiceFactoryBean"
		class="org.springframework.format.support.FormattingConversionServiceFactoryBean">
		<property name="converters">
			<list>
				<ref bean="employeeConvert" />
			</list>
			<!-- <list> <bean class="org.pan.springmvc.contvert.EmployeeConvert"></bean> 
				</list> -->
		</property>
	</bean>

	<!-- 使用 SessionLocaleResolver + LocaleChangeInterceptor拦截器的方式使用国际化 
	<bean id="localeResolver"
		class="org.springframework.web.servlet.i18n.SessionLocaleResolver"></bean>
	<mvc:interceptors>
		<bean id="localeChangeInterceptor"
			class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor"></bean>
	</mvc:interceptors>
	-->
	
	<!-- 自定义拦截器 -->
	<mvc:interceptors>
		<bean id="firstInterceptor" class="org.pan.springmvc.interceptor.FirstInterceptor"></bean>
		<!-- 配置多个拦截器的时候，还可以进行细粒度的配置，指定请求的路径，而触发 -->
		<mvc:interceptor>
		
			<mvc:mapping path="/testRestFul/emps"/>
			<!-- org.pan.springmvc.interceptor.SecondInterceptor -->
			<ref bean="secondInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>
	


	<!-- 配置国际化信息 -->
	<bean id="messageSource"
		class="org.springframework.context.support.ResourceBundleMessageSource">
		<property name="basename" value="i18n" />
	</bean>

	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="defaultEncoding" value="UTF-8" />
		<property name="maxInMemorySize" value="1048576" />
	</bean>

</beans>
